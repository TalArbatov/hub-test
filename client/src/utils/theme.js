const colors = {
    primary : '#3a2743',
    primaryHover : '#5f446b'
}

const theme = {
    colors: {
        primary: colors.primary,
        primaryHover: colors.primaryHover

    },
    button: {
        primary: {
            bgColor: colors.primary
        }
    },
    submitButton: {
        color: '#1ECD97',
    }
}

export default theme;